# Tab complete globs
setopt GLOB_COMPLETE

source ~/.zsh/terminal-same-directory-fix

cdpath=($HOME/Code)

# Increase ulimit for Gulp
ulimit -n 10240

export GOPATH=$HOME/code/personal/go-lab
export PATH=$GOPATH/bin:$PATH:/usr/local/mysql/bin

source $(brew --prefix nvm)/nvm.sh
